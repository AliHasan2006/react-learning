import React from 'react'

const UserContext = React.createContext()

export default UserContext;



/* this is part 1
context always give a major thing that is provider because context give you a variable (data). so every context is/has a provider. conext is global variable. provider is use wrapper to rap componenest like fragments <> </>.    
*/